package com.example.proyectoandroid_soler_martin_perez;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class usuario_creado extends AppCompatActivity {

    /*
     * Clase que hace referencia a una pantalla donde indica que el usuario se ha creado
     * en caso de que la creacion de usuarios haya funcionado
     */
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_usuario_creado);
    }
}